[project]
name = "phase3-team"
version = "0.1.0"
description = "Shared Isaac Lab Phase-3 surgical-instrument data generation"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"
